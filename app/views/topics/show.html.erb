<h1><%= @topic.name %></h1>

<% if user_is_authorized_for_topics? %>
  <%= link_to "Edit Topic", edit_topic_path, class: 'btn btn-success' %>
  <%= link_to "Delete Topic", @topic, method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this topic?' } %>
<% end %>

<div class="row">
  <div class="col-md-8">
    <p class="lead">
      <%= render partial: "labels/list", locals: { labels: @topic.labels } %>
      <br />
      <%= @topic.description %>
    </p>
    <%= render @topic.posts %>
  </div>
  <div class="col-md-4">
    <% if current_user %>
      <%= link_to "New Post", new_topic_post_path(@topic), class: 'btn btn-success' %>
    <% end %>
  </div>
</div>
<hr>
<div class="row">
  <div class="col-md-8">
    <h3>Comments</h3>
    <% @topic.comments.each do |comment| %>
      <%= render partial: 'comments/comment', locals: {item: @topic, comment: comment} %>
    <% end %>
    <% if current_user %>
      <%= render 'comments/form', comment: Comment.new, item: @topic %>
    <% end %>
  </div>
</div>

<!--<hr>-->
<!--<div class="row">-->
<!--  <div class="col-md-8">-->
<!--    <p class="lead">Sponsored Posts</p>-->
<!--    <% @topic.sponsored_posts.each do |sponsored_post| %>-->
<!--      <div class="media">-->
<!--        <div class="media-body">-->
<!--          <h4 class="media-heading">-->
<!--            $<%= sponsored_post.price %> -- -->
<!--            <%= link_to sponsored_post.title, topic_sponsored_post_path(@topic, sponsored_post) %>-->
<!--          </h4>-->
<!--        </div>-->
<!--      </div>-->
<!--    <% end %>-->
<!--  </div>-->
<!--  <div class="col-md-4">-->
<!--    <%= link_to "New Sponsored Post", new_topic_sponsored_post_path(@topic), class: 'btn btn-success' %>-->
<!--  </div>-->
<!--</div>-->
